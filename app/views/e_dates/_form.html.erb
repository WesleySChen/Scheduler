<%= form_for(@e_date) do |f| %>
  <% if @e_date.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@e_date.errors.count, "error") %> prohibited this e_date from being saved:</h2>

      <ul>
      <% @e_date.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :pos_date %><br>
    <%= f.date_select :pos_date %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
