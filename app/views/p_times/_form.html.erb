<%= form_for(@p_time) do |f| %>
  <% if @p_time.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@p_time.errors.count, "error") %> prohibited this p_time from being saved:</h2>

      <ul>
      <% @p_time.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :p_start %><br>
    <%= f.time_select :p_start %>
  </div>
  <div class="field">
    <%= f.label :p_end %><br>
    <%= f.time_select :p_end %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
